/**
 *
 * @param {Array} array - Source array
 * @returns {String} Merged text
 */
export function stringsArrayToString(array) {
  return array && Array.isArray(array) ? array.join(', ') : ''
}

/**
 * 
 * @param {String} text - Long text
 * @param {Number} size - Text cropped length  
 * @param {String} separator - Cropped to separator from end
 * @returns {String} Cropped string
 */
export function croppingTextToSeparator(text, size, separator) {
  let prepare = ''
  if (separator) {
  }

  return prepare
}
